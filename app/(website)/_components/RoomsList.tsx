// import { getRooms } from "@/lib/roomsApi";
// import DisplayedRooms from "./DisplayedRooms";

// export default async function RoomsList() {
//   const { success, rooms } = await getRooms();

//   if (!success || !rooms || rooms.length === 0) {
//     return (
//       <div className="-mt-5 text-slate-400">
//         No rooms available at the moment. Please check back later.
//       </div>
//     );
//   }

//   return <DisplayedRooms rooms={rooms} />;
// }
