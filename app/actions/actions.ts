"use server";

export async function createBooking(formData: FormData) {
  //remove reserve button if start from is not available
  console.log(formData);

  //status: unconfirmed
  //hasBreakfast: true, if extraPrice is >1
  //isPaid: false
  //hasBreakfast
}
